#!/usr/bin/env python

import numpy
import random

class dungen
    def init(self):
        maxx = 100
        maxy = 100
        genx = 50
        geny = 50
        map = numpy.ones((self.ymax, self.xmax))
        random.seed(

    def create(self):
         
        self.fillRect(x, y, size)
